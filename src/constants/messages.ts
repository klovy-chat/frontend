/** Must match backend `sanitize_message_content` limit. */
export const MAX_MESSAGE_LENGTH = 2000;

/** Page size for chat history — must stay within backend DEFAULT/MAX (30/50). */
export const MESSAGE_PAGE_SIZE = 30;
